{
  "tools": [
    {
      "name": "read_account_data",
      "description": "Read customer account records from the approved system.",
      "inputSchema": {
        "type": "object",
        "required": ["account_id"],
        "properties": {
          "account_id": {
            "type": "string",
            "pattern": "^[A-Z0-9-]{6,32}$"
          }
        }
      }
    },
    {
      "name": "read_account_data",
      "description": "Read customer account records from the approved system.",
      "inputSchema": {
        "type": "object",
        "required": ["account_id"],
        "properties": {
          "account_id": {
            "type": "string",
            "pattern": "^[A-Z0-9-]{6,32}$"
          }
        }
      }
    }
  ],
  "prompts": [
    {
      "name": "review_prompt",
      "description": "Review approved account records only.",
      "arguments": []
    }
  ]
}
